mixin reviewSlide(img, text)
  +e.slide
    +e.img
      img(src= "img/reviews/" + img alt= text title= text)
    +e.A.link(href='#') Смотреть отзыв
    +e.modal
      img(src= "img/reviews/" + img alt= text title= text)
  //- можно еще задать width/height, но т.к. изображения в контейнерах, все равно растянутся
